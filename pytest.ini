[pytest]
testpaths = tests
markers =
    unit: pure unit tests (no external boundaries)
    integration: integration tests across modules and persistence
    contract: API and schema contract tests
    e2e: end-to-end API journey tests
    chaos: state-mutation and race-style behavior tests
    slow: high-cost tests (volume/performance/stress)

